import { RutinaBicepPipe } from './rutina-bicep.pipe';

describe('RutinaBicepPipe', () => {
  it('create an instance', () => {
    const pipe = new RutinaBicepPipe();
    expect(pipe).toBeTruthy();
  });
});
